const list=document.getElementById('food-moments-list');const esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));async function load(){try{const r=await AuthShell.request('/food-moments');const items=await r.json();if(!r.ok)throw new Error(items?.error||'Food Moments konnten nicht geladen werden.');if(!items.length){list.innerHTML='<div class="moments-empty"><h2>Noch kein Food Moment.</h2><p>Der erste darf ganz klein anfangen.</p><a href="/foodMomentCreate.html">Food Moment erstellen →</a></div>';return;}list.innerHTML=items.map(m=>`<a class="moment-list-card" href="/foodMoment.html?id=${encodeURIComponent(m.public_id)}"><div><span>${m.moment_date?new Date(m.moment_date+'T12:00:00').toLocaleDateString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit'}):'Offen'}</span><h2>${esc(m.title)}</h2><p>${esc(m.recipes?.[0]?.name||m.inspirations?.[0]?.title||'Noch offen')}</p></div><strong>→</strong></a>`).join('');}catch(e){list.innerHTML=`<p>${esc(e.message)}</p>`;}}document.addEventListener('auth:ready',load);if(window.AuthShell?.isReady?.())load();
