(function(){
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function metric(label,value,note=''){return `<div class="admin-summary-card"><span>${esc(label)}</span><strong>${esc(value)}</strong>${note?`<small>${esc(note)}</small>`:''}</div>`;}
async function load(){const s=document.getElementById('analytics-summary');s.innerHTML='<p>Lädt…</p>';const r=await AuthShell.request('/analytics/summary?days=28');if(!r.ok){s.innerHTML='<p>Auswertung konnte nicht geladen werden. Platform-Admin-Rechte erforderlich.</p>';return;}const d=await r.json();const t=d.totals||{};const share=t.total_events?Math.round((t.connection_events/t.total_events)*100):0;s.innerHTML=metric('Aktive Nutzer',t.active_users||0)+metric('Sessions',t.sessions||0)+metric('Aktionen',t.total_events||0)+metric('Connections',t.connection_events||0,`${share}% aller Aktionen`);const names={inspiration:'Inspiration festhalten',recipe_planning:'Rezept konkret einplanen',occasion:'Besuch / Anlass',weekly_planning:'Woche planen',shopping:'Einkaufen'};document.getElementById('analytics-journeys').innerHTML=Object.entries(d.journey_signals||{}).map(([k,v])=>`<div class="admin-result-row"><strong>${esc(names[k]||k)}</strong><span>${esc(v)} Signale</span></div>`).join('')||'<p>Noch keine Signale.</p>';document.getElementById('analytics-events').innerHTML=(d.events||[]).map(e=>`<div class="admin-result-row"><div><strong>${esc(e.event_name)}</strong><small>${esc(e.event_category)}</small></div><span>${esc(e.count)} · ${esc(e.users)} Nutzer</span></div>`).join('')||'<p>Noch keine Ereignisse.</p>';}
document.addEventListener('auth:ready',load);document.getElementById('analytics-refresh')?.addEventListener('click',load);})();
